/*
             Timestamp  : 09:56 - Tuesday  03,Dec 2024   
*/

#include <bits/stdc++.h>
// This line includes a precompiled header that includes most of the standard C++ libraries. It is commonly used in competitive programming to save time.

using namespace std;
// This line allows us to use all the standard library names without prefixing them with "std::".

int main()
// This is the main function where the execution of the program begins.

{
    string s; 
    // This line declares a variable 's' of type string to store the input.

    cin >> s;
    // This line reads a string from the standard input (usually the keyboard) and stores it in the variable 's'.

    cout << "Hello, " << s << "\n";
    // This line prints "Hello, " followed by the value of 's' and a newline character to the standard output (usually the screen).

    return 0;
    // This line returns 0 from the main function, indicating that the program ended successfully.
}